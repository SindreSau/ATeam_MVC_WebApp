@model FoodCategory
@{
    ViewData["Title"] = "Edit Category";
    Layout = "_Layout";
}

<div class="container py-4">
    <a asp-controller="Category" asp-action="Index" class="link-info mb-4">← Back to Categories</a>

    <div class="card mt-4">
        <div class="card-header">
            <h2 class="h4 card-title mb-1">Edit Category</h2>
        </div>
        <div class="card-body">
            <form asp-controller="Category" asp-action="Update" method="post">
                <input type="hidden" asp-for="FoodCategoryId" />
                <input type="hidden" asp-for="CreatedAt" />

                <div class="mb-4">
                    <label asp-for="CategoryName" class="form-label">Category Name</label>
                    <input asp-for="CategoryName" class="form-control" required maxlength="100" />
                    <span asp-validation-for="CategoryName" class="text-danger"></span>
                </div>

                <div class="mt-4">
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save me-1"></i>
                        Save changes
                    </button>
                    <a asp-controller="Category" asp-action="Index" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}